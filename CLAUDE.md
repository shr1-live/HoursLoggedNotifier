# Project instructions

- Always push committed changes to `origin/main` right after committing — don't wait for a separate "push" request. Still follow normal git safety (new commits, no force-push, no `--no-verify`).
